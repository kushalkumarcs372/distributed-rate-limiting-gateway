apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: rate-limiter
resources:
  - namespace.yaml
  - postgres.yaml
  - redis.yaml
  - gateway.yaml
  - loadbalancer.yaml
# To deploy the image CI pushes to GHCR instead of a locally loaded one:
# images:
#   - name: rate-limiting-gateway
#     newName: ghcr.io/kushalkumarcs372/rate-limiting-gateway
#     newTag: latest
